---
"dojocho": minor
"@dojocho/config": patch
"@dojocho/effect-ts": patch
---

Ship the production Dojo web UI inside the installable CLI, launch it without a monorepo checkout, and include machine-readable lesson checks with coverage support.
